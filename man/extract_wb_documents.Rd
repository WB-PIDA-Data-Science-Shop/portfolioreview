% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wb_documents.R
\name{extract_wb_documents}
\alias{extract_wb_documents}
\title{Extract World Bank API documents from JSON}
\usage{
extract_wb_documents(json)
}
\arguments{
\item{json}{A list from \code{httr2::resp_body_json()}. See \code{\link{fetch_wb_documents_json}}.}
}
\value{
A tibble with one row per document. The \code{authors} column is a single
character string with names separated by semicolons. Rows with missing \code{id}
are removed.
}
\description{
Extracts documents from a World Bank API JSON response, collapses the nested
\code{authors} field to a semicolon-separated string, and returns a tibble with
one row per document.
}
\examples{
\dontrun{
resp_json <- fetch_wb_documents_json(os = 0, rows = 100)
docs_tbl <- pluck_wb_documents(resp_json)
}
}
